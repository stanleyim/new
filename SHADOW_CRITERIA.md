# 섀도 포워드 판정 기준 (2026-09-20 확정, 이후 변경 금지)

## 목적
ML 종목 점수를 실돈 없이 매일 기록해, 앞으로의 성과로 "ML을 추가한 혼합안이 v7 단독보다 낫다"를 판정한다.
백테스트(2021~2026)는 혼합 비율을 고르는 데 이미 쓰여서 깨끗한 증거가 아니다. 이 기록이 유일한 사후 검증이다.

## 고정 사항
- 기록기: `shadow_ml.py`, `.github/workflows/shadow_ml.yml`. 모델·파라미터·피처는 바꾸지 않는다.
- 재학습: 2027-01-01에만 규칙대로 한다. 학습은 (해당 연도 1월 1일 - 35일) 이전 데이터만 쓴다.
- 기록 시작: 2026-09-18.
- 평가 환경: pandas 2.2.3(동점 종목 정렬 순서가 pandas 버전에 따라 v7 결과를 바꾸므로 고정).
- 평가 정의는 백테스트와 같다. T+1 시가 진입, T+20 종가 청산, 왕복 비용 0.206%, 20일 IC, 십분위 스프레드(최상위-최하위), ML 상위 10종목 포트폴리오, 혼합안 = ML 50% + v7 50%.
- v7 단독은 같은 환경에서 재계산한다. 실제 기록된 v7(output/signals)과 다르면 차이를 함께 보고한다.

## 점검 시점
- 기준은 20일 뒤 수익이 확정된 기록일 수다.
- 중간 점검: 100일. 최종 판정: 250일.

## 중간 점검 (100일)
- IC ≤ 0 이고 스프레드 ≤ 0 이 동시에 성립하면 ML 폐기. 아니면 계속.

## 최종 판정 (250일)
- A. 선택력: 20일 IC 평균 ≥ 0.035 이고 스프레드 > 0.
- B. 포트폴리오: 혼합안 Sharpe > v7 단독 Sharpe 이고, 혼합안 MDD가 v7 단독 MDD보다 얕다.

| A | B | 결정 |
|---|---|---|
| 통과 | 통과 | ML을 실전 포함 후보로 올린다. 실돈 투입은 별도 결정. |
| 통과 | 실패 | 125일 연장, 1회만. 같은 기준으로 재판정. 다시 실패하면 폐기. |
| 실패 | - | ML 폐기, v7 단독 유지. |

## 무효 조건
- 기록일이 해당 기간 거래일의 90% 미만이면 판정하지 않고 원인부터 조사한다.

## 알려진 한계
- 20일 보유가 겹쳐 250일이어도 독립 구간은 약 12개뿐이다.
- IC 0.035는 백테스트 전체 IC(0.073)의 약 절반으로 정한 값이다. 그 이상의 근거는 없다.
- 통과해도 수익이 보장되지 않는다.
- 기준을 바꾸려면 결과를 본 뒤가 아니라 기록을 새로 시작해야 한다.

## 변경 기록 (본문 기준은 바꾸지 않음)
- 2026-09-22: 기록 CSV(shadow_ml)에 `name`(종목명) 열 추가 — 보기 위한 열이며 예측·풀·모델·지표·판정 기준과 무관. 2026-09-21 기록 파일에는 없다(기록 파일은 수정하지 않는다).

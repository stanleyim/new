# 추가 KRX 자료 수집 (성능 개선 연구용)

## 목적
지금 ML은 가격·수급·공매도만 쓴다. v7보다 훨씬 큰 우위를 찾으려면 새 정보가 필요하다는 판단으로,
KRX에서 시점 그대로(과거 값이 사후에 안 바뀜) 받을 수 있는 자료를 추가로 모은다.
`pit_data`(기존, 시세·수급·공매도)는 건드리지 않는다. 새 자료는 같은 `pit-data` 브랜치의 `pit_data_extra/`에 별도로 쌓는다.

## 받는 자료 (모두 일별, 전 종목)
- `fund_YYYY.parquet`: PER, PBR, EPS, BPS, 배당수익률, DPS
- `forgn_YYYY.parquet`: 외국인 보유수량·지분율·한도소진율
- `sbal_YYYY.parquet`: 공매도 잔고(수량·금액·비중), KOSPI·KOSDAQ
- `inv_YYYY.parquet`: 세부 투자자 8종(금융투자·보험·투신·사모·은행·기타금융·연기금·기타외국인) 순매수거래대금 — 기존 `flow`의 4종(기관합계·개인·외국인·기타법인)을 세분화한 것
- `sector_YYYY.parquet`: 업종 분류, 매월 첫 거래일 1회(업종은 자주 안 바뀜)
- `idx_YYYY.parquet`: 코스피·코스닥·코스피200 일봉, 연 1회 통째로

## 만들지 않은 것과 이유
- 재무제표(분기 실적): pykrx에 없다. 크롤링이 필요해 이번 주 범위를 벗어난다고 보고 뺐다.
- 뉴스·공시 텍스트: 정형 자료가 아니라 별도 파이프라인이 필요해서 뺐다.
- 이 둘이 필요하면 말씀해 주시면 별도로 계획을 짜겠습니다.

## 상장폐지 종목
`get_market_fundamental_by_ticker` 등은 그날 시점 상장 종목만 반환한다(ALL 시장). 상장폐지 종목도
살아있던 기간은 정상적으로 포함되므로 기존 `pit_data`와 같은 방식으로 시점 그대로다.

## 실행
1. `pit_extra.py`, `.github/workflows/pit_extra_collect.yml`을 레포에 커밋.
2. Actions → "PIT Extra Data Collect (manual)" → Run workflow. 연도별(2014~2026)로 최대 3개씩 병렬 실행.
3. 실패한 연도는 `failed_extra_YYYY.txt`를 보고 필요하면 그 연도만 재실행.

## 시험한 것
- 가짜 KRX 응답으로 정상·결측일(휴장 아닌데 빈 결과)·호출 예외 3가지를 시험. 실패한 항목만 스킵하고 나머지는 정상 저장됨을 확인.
- 진짜 KRX 호출은 못 해봤다(sandbox에서 접속 불가). 연도당 거래일 250개 × 항목 8개 × 재시도 여지가 있어, `pit_collect.py`보다 호출이 훨씬 많다. 시간이 오래 걸리거나 KRX가 막을 수 있다. 처음엔 연도 1~2개만 수동 실행해서 시간과 실패율을 보고, 문제없으면 나머지를 돌리는 걸 권한다.

## 이다음
자료가 다 모이면(파일 존재만으로는 부족, 값이 맞는지 확인 필요) 제가 검증 실험 계획(설계 구간/시험 구간 분리, 사전 고정 지표)을 짜서 형 승인을 받고 진행하겠습니다.
